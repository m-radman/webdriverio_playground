const BASE_URL = "https://the-internet.herokuapp.com"

describe("Inputs tests", () => {
  it("user should be able to set input 0", async () => {
    await browser.url(`${BASE_URL}/checkboxes`)
    // ...  
  })  
})
